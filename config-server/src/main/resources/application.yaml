server:
  port: 8888

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/droveda/microservice-study
          search-paths: microservice-repo
        #native:
        #  search-locations: file:///Users/diegues/microservice-repo
  #profiles:
  #  active: native


